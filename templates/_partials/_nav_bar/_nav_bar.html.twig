<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">
      <img src="{{ asset('assets/img/logo.png') }}" alt="Logo" height="90">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
            aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">

      {# --- MENU PRINCIPAL (centré) --- #}
      <ul class="navbar-nav mx-auto gap-lg-3 fs-5 fw-semibold">
        <li class="nav-item">
          <a class="nav-link d-flex align-items-center gap-2" href="{{ path('app_home') }}">
            <i class="bi bi-house-door"></i> Accueil
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link d-flex align-items-center gap-2" href="#">
            <i class="bi bi-journal-text"></i> Tous les menus
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link d-flex align-items-center gap-2" href="{{ path('app_contact') }}">
            <i class="bi bi-envelope"></i> Contact
          </a>
        </li>
      </ul>

      {# --- COMPTE À DROITE (tout regroupé ici) --- #}
      <ul class="navbar-nav ms-lg-auto fs-5 fw-semibold">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle d-flex align-items-center gap-2"
             data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
            <i class="bi bi-person-circle"></i> Compte
          </a>

          <div class="dropdown-menu dropdown-menu-end">

            {% if app.user %}
              {# --- SECTION UTILISATEUR --- #}
              <h6 class="dropdown-header">Espace utilisateur</h6>

              <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_profile') }}">
                <i class="bi bi-person"></i> Mon profil
              </a>

              <a class="dropdown-item d-flex align-items-center gap-2" href="#">
                <i class="bi bi-bag-check"></i> Mes commandes
              </a>

              {# --- SECTION EMPLOYÉ --- #}
              {% if is_granted('ROLE_EMPLOYEE') %}
                <div class="dropdown-divider"></div>
                <h6 class="dropdown-header">Espace employé</h6>

                <a class="dropdown-item d-flex align-items-center gap-2" href="#">
                  <i class="bi bi-briefcase"></i> Tableau employé
                </a>
              {% endif %}

              {# --- SECTION ADMIN --- #}
              {% if is_granted('ROLE_ADMIN') %}
                <div class="dropdown-divider"></div>
                <h6 class="dropdown-header">Espace administrateur</h6>
<a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('admin_menu_index') }}">
  <i class="bi bi-journal-text"></i> Gestion des menus
</a>

                <a class="dropdown-item d-flex align-items-center gap-2" href="#">
                  <i class="bi bi-shield-lock"></i> Tableau admin
                </a>

                <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('admin_opening_hour_index') }}">
                  <i class="bi bi-clock-history"></i> Gestion des horaires
                </a>
              {% endif %}

              <div class="dropdown-divider"></div>

              <a class="dropdown-item d-flex align-items-center gap-2 text-danger" href="{{ path('app_logout') }}">
                <i class="bi bi-box-arrow-right"></i> Déconnexion
              </a>

            {% else %}
              <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_login') }}">
                <i class="bi bi-box-arrow-in-right"></i> Connexion
              </a>

              <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_register') }}">
                <i class="bi bi-person-plus"></i> Créer un compte
              </a>
            {% endif %}

          </div>
        </li>
      </ul>

    </div>
  </div>
</nav>
